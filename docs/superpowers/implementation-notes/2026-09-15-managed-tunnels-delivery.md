# 服务器管理隧道交付记录

程序版本：`4e7ae940963084f22f9ed6add9ee595d05f6dddb`。服务器启用独立 managed-tunnels SQLite 数据库，静态及动态客户端共享管理入口；TCP、UDP、P2P 导出及转发均支持保存、删除和重连应用。客户端 GUI 保留服务器管理的配置，避免写回本地配置。

用户反馈修复：转发卡片容器采用顶部对齐并自动换行；服务器端口占用返回独立错误码 11，权限不足返回 12。客户端界面及管理报告共用中文说明。旧协议继续返回兼容的通用拒绝码，新客户端解释其含义，不推断成端口占用。

验证：协议测试 46 项、GUI 测试 86 项、服务端控制集成测试 17 项、客户端控制集成测试串行运行 18 项通过。中文原因单测通过。工作区 clippy（所有 target，警告视为错误）、格式检查通过，Windows 和 Linux release 构建完成。此前工作区测试发现 Web 资源路径白名单计数未更新，修正后相关 Web 测试 18 项通过。一次并发执行中的提供方协议切换测试达到 20 秒总超时，单独复测及完整串行执行通过；保留原失败日志。

部署备份：`/opt/rustgo/backups/managed-20260915-20260915T034651Z`，包含配置、原二进制和状态数据库。服务器健康检查返回 200。本地 `deploy/client/rustgoc-gui.exe` 已更新并打开。服务器 UDP 监听地址根据实际网卡设置为 `172.24.36.113`。

用户最后保存的隧道配置保持不变：22 使用 10022，33 使用 10023，目标均为 `127.0.0.1:22`。10022 由原 frps 占用，不停止该服务。本地检查未发现 TCP 22 监听；隧道注册成功不代表目标 SSH 服务已启动。

详细构建、回归测试及线上验证记录保存在忽略的 `test-results/` 目录，部署配置及凭据未提交。

线上验证完成：临时 TCP/UDP 隧道和 P2P 导出、转发均通过管理 API 新增与删除；TCP 和 UDP 实际数据回显成功，离线目标转发显示等待及中文原因。测试项目全部删除，服务器配置与用户基线一致，最终修订号 17，客户端在线。初次验证暴露 UDP 监听配置缺失，补齐后重新验证通过。
